import { type SectionBackgroundTheme } from './types';

/** Univers Accueil — ambiance corail / lever de soleil (chaleur, optimisme, découverte). */
export const HomeTheme: SectionBackgroundTheme = {
  key: 'accueil',
  background: require('@/assets/themes/home-background.webp'),
  veil: '#C6553F',
  veilOpacity: 0.3,
};
