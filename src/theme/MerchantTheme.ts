import { type SectionBackgroundTheme } from './types';

/** Univers Commerçants — ambiance terracotta / rue commerçante (artisanat, authenticité). */
export const MerchantTheme: SectionBackgroundTheme = {
  key: 'commerce',
  background: require('@/assets/themes/merchants-background.webp'),
  veil: '#B5533A',
  veilOpacity: 0.3,
};
