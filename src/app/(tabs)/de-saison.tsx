import { SaisonScreen } from '@/features/carnet/components/SaisonScreen';

/** Onglet « De saison » (Carte de saison du Carnet). */
export default function DeSaisonTab() {
  return <SaisonScreen />;
}
